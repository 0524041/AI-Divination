[project]
name = "divination-app"
version = "0.4.0"
description = "AI Divination App with Multi-User Support"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "flask",
    "google-genai",
    "bcrypt",
    "cryptography"
]
