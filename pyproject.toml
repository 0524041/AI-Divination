[project]
name = "divination-app"
version = "0.4.0"
description = "AI Divination App with Multi-User Support"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "flask",
    "google-genai",
    "bcrypt",
    "cryptography",
    "flask-cors>=6.0.2",
    "lunar-python>=1.4.8",
]
